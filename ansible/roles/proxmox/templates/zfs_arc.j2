# NOTE: THIS FILE IS MANAGED BY ANSIBLE
#
# Bound the ZFS ARC so the hypervisor's cache does not compete with the VMs for RAM.
# The kernel reads these at module load, so changes apply on the next boot.

options zfs zfs_arc_min={{ proxmox_zfs_arc_min }} zfs_arc_max={{ proxmox_zfs_arc_max }}
