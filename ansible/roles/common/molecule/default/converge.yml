---
- name: Converge
  hosts: all
  become: true
  vars_files:
    # Single source of truth for admin_users (used by the common role).
    - "{{ lookup('env', 'MOLECULE_PROJECT_DIRECTORY') }}/../../group_vars/all"
  roles:
    - common # noqa syntax-check[specific]
